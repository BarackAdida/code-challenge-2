// initial line which allows the result to be in array form
const resultArray =[];
// for returns the numbers between 4 to 15
for (let number = 4; number <= 15; number++) {
    // the .push() ensures that the result from the for is returned in array
    resultArray.push(number)
}
console.log(`${resultArray}`)
